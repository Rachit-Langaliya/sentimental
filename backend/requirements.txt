# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.30.0
alembic==1.13.3
psycopg2-binary==2.9.9
pgvector==0.3.5

# Validation & config
pydantic==2.9.2
pydantic-settings==2.5.2

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Task queue
celery[redis]==5.4.0
redis==5.1.1

# HTTP client
httpx==0.27.2

# Utilities
python-dotenv==1.0.1
structlog==24.4.0
langdetect==1.0.9

# ── ML / NLP (Phase 3) ────────────────────────────────────────────────────────
# Install these separately or via: pip install -r requirements-ml.txt
# Kept here for completeness; CI can skip with --no-deps if no GPU available.
torch==2.4.1+cpu
transformers==4.44.2
sentence-transformers==3.0.1
bertopic==0.16.4
hdbscan==0.8.40
umap-learn==0.5.6
numpy==1.26.4
scikit-learn==1.5.2
